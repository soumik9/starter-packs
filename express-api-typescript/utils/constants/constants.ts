export enum ENUM_USER_ROLE {
    ADMIN = 'admin',
    CUSTOMER = 'customer',
    SUPPLIER = 'supplier'
}

// pagination props
export const paginationProps: string[] = ['page', 'limit', 'sortBy', 'sortOrder'];